export const environment = {
  production: false,
  apiBaseUrl: 'http://127.0.0.1:8000/api',
  // Toggle to enable/disable remote API requests during development.
  // Set to `true` to attempt network fetches; `false` will use localStorage only.
  useApi: false
};
