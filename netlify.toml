[build]
  command = "npm run build"
  publish = "dist/Projects"

[context]
  # Optional: you can set environment-specific overrides here
  # [context.production.environment]
  #   NODE_ENV = "production"
